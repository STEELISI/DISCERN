#!/bin/bpftrace

tracepoint:syscalls:sys_enter_socketpair
{
    @[pid, uid, gid, args->family, args->type, args-> protocol] = count();
}

interval:s:60 { 
    time();
    print(@); 
    clear(@); 
}

